# Copy to .env and fill in your own values:  cp .env.example .env
#
# Required only for --browser-auto-login. .env is gitignored; never commit it.
ICPSR_EMAIL=you@example.com
ICPSR_PASSWORD=your-password