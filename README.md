# REPRO-taskgen source collection

```bash
./run_script.sh  # run every source scraper in metadata-only

./run_script.sh --start-date 2025-01-01 --end-date 2025-12-31 # try different start and end dates

./run_script.sh --plan  # preview all commands without writing outputs

```
